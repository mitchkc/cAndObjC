#include <stdio.h>
#include <stdlib.h>
#include <math.h>
/*
int main(){

    double num1;
    double num2;
    char op;

    printf("Enter a number: ");
    scanf("%lf", &num1);                    //Don't forget the L in %lf!!!!
    printf("Enter operation to perform: ");
    scanf(" %c", &op);                      //space before %c!!!!!!
    printf("Enter another number: ");
    scanf("%lf", &num2);

    if(op == '+'){
        printf("%f", num1+num2);
    }
    else if(op == '-'){
        printf("%f", num1-num2);
    }
    else if(op == '/'){
        printf("%f", num1/num2);
    }
    else if(op == '*'){
        printf("%f", num1*num2);
    }
    else{
        printf("Invalid operator!");
    }

    return 0;
}
*/
