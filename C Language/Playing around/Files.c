#include <stdio.h>
#include <stdlib.h>
#include <math.h>
/*
int main(){

    /*
    FILE * fPointer = fopen("employees.txt", "w");  //fopen("fileName", fileMode(r - read, w - write, a - append))
    //if you don't specify an absolute path the file will be created wherever you are
    fprintf(fPointer, "Michael Connolly - graduate\nTeresa Rafferty - Typist");  //write info to file or overwrite

    FILE * fPointer = fopen("employees.txt", "a");

    fprintf(fPointer, "\nKelly - sales");

    fclose(fPointer);   //always close file


    //READ FROM FILE
    char line[255];

    FILE * fPointer = fopen("employees.txt", "r");

    fgets(line, 255, fPointer);   //fgets(where to store?, size?, read what?)
    fgets(line, 255, fPointer); //now incremented to 2nd line in file
    printf("%s", line);


    return 0;
}
*/
