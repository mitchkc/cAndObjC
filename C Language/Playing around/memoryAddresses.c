#include <stdio.h>
#include <stdlib.h>
#include <math.h>
/*
int main(){

    int age = 31;
    double gradeP = 70.0;
    char grade = 'A';

    printf("Age = %p \nGrade Percent = %p \nGrade = %p", &age, &gradeP, &grade);  //p = pointer


    //POINTERS
    //a pointer is a type of data, it is a memory location
    int age = 31; //integer variable with an assigned integer value
    //when creating a pointer variable we store the memory address of an already existing variable
    int * pAge = &age;

    double grade = 70.0;
    double * pGrade = &grade;

    char grading = 'A';
    char * pGrading = &grading;

    printf("Age's memory location is: %p\n", &age);


    //DEREFERENCING POINTERS
    int age = 31;
    int *pAge = &age;
    printf("memory location: %p\n", &age);
    printf("Actual value: %d", *pAge);   //dereference pointer variable and retrieving actual value stored there


    return 0;
}
*/
