#include <stdio.h>
#include <stdlib.h>
#include <math.h>
/*
typedef struct Student{

    char name[20];
    char degree[20];
    int age;
    double grade;

}Student_t;


int main(){

    // Structs are data structures where we can store groups of data types. Objects in Java!

    //struct Student studentOne;
    student_t studentOne;
    studentOne.age = 31;
    //studentOne.name = "Michael Connolly"; This is wrong because we are working with an array of chars!!!
    strcpy(studentOne.name, "Michael C"); //string copy function
    strcpy(studentOne.degree, "Software");
    studentOne.grade = 70.0;

    printf("%s, %d, %s, %f", studentOne.name, studentOne.age, studentOne.degree, studentOne.grade);

    struct Student studentTwo;
    studentTwo.age = 31;
    //studentTwo.name = "Michael Connolly"; This is wrong because we are working with an array of chars!!!
    strcpy(studentTwo.name, "Michael C"); //Must use string copy function
    strcpy(studentTwo.degree, "Software");
    studentTwo.grade = 70.0;

    return 0;
}
*/

