#include <stdio.h>
#include <stdlib.h>
#include <math.h>

/*
int main(){


    //GUESS game
    int secretNum = 5;
    int guess;

    while(guess != secretNum){
        printf("Enter your guess: ");
        scanf("%d", &guess);
    }
    printf("You guessed correctly!");


    //GAME with a guess limit!
    int secretNum = 5;
    int guess;
    int guessCount = 0;
    int guessLimit = 3;
    int OutOfGuesses = 0;

    while(guess != secretNum && OutOfGuesses == 0){
            if(guessCount < guessLimit){
                printf("Enter your guess: ");
                scanf("%d", &guess);
                guessCount++;
            }
            else{
                OutOfGuesses = 1;
            }
    }

    if( OutOfGuesses == 1){
        printf("Out of guesses!");
    }
    else {
        printf("You guessed correctly!");
    }



    return 0;
}
*/
